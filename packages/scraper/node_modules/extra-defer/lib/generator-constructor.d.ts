import { GeneratorExecutor, ICallback } from './generator-executor.js';
export declare class GeneratorConstructor<Yield = unknown, Next = unknown, Args extends unknown[] = []> extends GeneratorExecutor<Yield, Next, Args> {
    protected iterate(callbacks: Array<ICallback<Yield, Next, Args>>): ICallback<Yield, Next, Args>[];
}
