import { IBlockingChannel } from "../utils/types.js";
export { ChannelClosedError } from "../utils/errors.js";
export declare class BufferedChannel<T> implements IBlockingChannel<T> {
    private bufferSize;
    private fsm;
    private buffer;
    private enqueueDeferredGroup;
    private dequeueDeferredGroup;
    constructor(bufferSize: number);
    send(value: T): Promise<void>;
    receive(): AsyncIterable<T>;
    close(): void;
}
