export async function everyAsync(iterable, predicate) {
    let index = 0;
    for await (const element of iterable) {
        if (!await predicate(element, index))
            return false;
        index++;
    }
    return true;
}
//# sourceMappingURL=every-async.js.map