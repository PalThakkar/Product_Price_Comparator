export function lazy(getter) {
    let exists = false;
    let value;
    return () => {
        if (!exists) {
            value = getter();
            exists = true;
        }
        return value;
    };
}
//# sourceMappingURL=lazy.js.map