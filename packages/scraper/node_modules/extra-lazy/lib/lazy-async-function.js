import { lazy } from './lazy.js';
export function lazyAsyncFunction(getter) {
    const getFunction = lazy(getter);
    return async (...args) => {
        const fn = await getFunction();
        return fn(...args);
    };
}
//# sourceMappingURL=lazy-async-function.js.map