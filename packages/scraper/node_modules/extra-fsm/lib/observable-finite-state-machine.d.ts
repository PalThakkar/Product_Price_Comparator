import { FiniteStateMachine } from './finite-state-machine.js';
import { Observable } from 'rxjs';
export interface IFiniteStateMachineStateChange<State extends string | number | symbol, Event extends string | number | symbol> {
    event: Event;
    oldState: State;
    newState: State;
}
export declare class ObservableFiniteStateMachine<State extends string | number | symbol, Event extends string | number | symbol> extends FiniteStateMachine<State, Event> {
    private stateChanges;
    get [Symbol.toStringTag](): string;
    observeStateChanges(): Observable<IFiniteStateMachineStateChange<State, Event>>;
    send(event: Event): void;
}
