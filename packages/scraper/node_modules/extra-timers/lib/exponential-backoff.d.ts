export declare function calculateExponentialBackoffTimeout({ baseTimeout, retries, maxTimeout, factor, jitter }: {
    baseTimeout: number;
    retries: number;
    maxTimeout?: number;
    factor?: number;
    jitter?: boolean;
}): number;
