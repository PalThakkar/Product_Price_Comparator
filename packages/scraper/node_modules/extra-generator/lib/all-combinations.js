import { allIndexCombinations } from './all-index-combinations.js';
import { map } from 'iterable-operator';
export function allCombinations(arr, k) {
    return map(allIndexCombinations(arr, k), indexes => indexes.map(i => arr[i]));
}
//# sourceMappingURL=all-combinations.js.map