import { isFunction, isntNull, isntUndefined } from 'extra-utils';
export function isIterable(val) {
    return isntNull(val)
        && isntUndefined(val)
        && isFunction(val[Symbol.iterator]);
}
export function isntIterable(val) {
    return !isIterable(val);
}
//# sourceMappingURL=is-iterable.js.map