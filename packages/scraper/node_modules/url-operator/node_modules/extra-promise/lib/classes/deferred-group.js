export class DeferredGroup {
    constructor() {
        this.group = new Set();
    }
    add(deferred) {
        this.group.add(deferred);
    }
    remove(deferred) {
        this.group.delete(deferred);
    }
    clear() {
        for (const deferred of this.group) {
            this.group.delete(deferred);
        }
    }
    resolve(value) {
        for (const deferred of this.group) {
            deferred.resolve(value);
        }
    }
    reject(reason) {
        for (const deferred of this.group) {
            deferred.reject(reason);
        }
    }
}
//# sourceMappingURL=deferred-group.js.map