import { assert } from '@blackglory/errors';
import { flattenByAsync } from './flatten-by-async.js';
export function flattenDeepAsync(iterable, depth = Infinity) {
    assert(depth === Infinity || Number.isInteger(depth), 'The parameter depth must be an integer');
    assert(depth >= 0, 'The parameter depth must be greater than or equal to 0');
    return flattenByAsync(iterable, (_, level) => level <= depth);
}
//# sourceMappingURL=flatten-deep-async.js.map