export function* tap(iterable, fn) {
    let index = 0;
    for (const element of iterable) {
        fn(element, index);
        yield element;
        index++;
    }
}
//# sourceMappingURL=tap.js.map