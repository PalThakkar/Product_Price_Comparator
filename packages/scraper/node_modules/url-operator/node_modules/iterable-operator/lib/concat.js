import { go } from '@blackglory/go';
export function concat(iterable, ...otherIterables) {
    return go(function* () {
        for (const iter of [iterable, ...otherIterables]) {
            yield* iter;
        }
    });
}
//# sourceMappingURL=concat.js.map