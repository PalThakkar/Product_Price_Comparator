import { ITransport, IMessage } from "../types.js";
export interface ITerminalTransportOptions {
    logMinimumDuration?: number;
}
export declare class TerminalTransport implements ITransport {
    private options;
    private id;
    constructor(options?: ITerminalTransportOptions);
    send(message: IMessage): void;
    private stringifyMessage;
    private createId;
}
