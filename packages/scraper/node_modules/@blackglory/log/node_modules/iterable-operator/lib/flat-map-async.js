export async function* flatMapAsync(iterable, fn) {
    let index = 0;
    for await (const element of iterable) {
        yield* await fn(element, index);
        index++;
    }
}
//# sourceMappingURL=flat-map-async.js.map