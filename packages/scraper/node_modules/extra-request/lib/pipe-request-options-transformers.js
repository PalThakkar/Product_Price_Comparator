import { Headers } from 'extra-fetch';
import { isntFalsy } from '@blackglory/prelude';
import { pipe } from 'extra-utils';
export function pipeRequestOptionsTransformers(...transformers) {
    const base = {
        url: new URL(typeof document !== 'undefined'
            ? document.baseURI
            : 'http://localhost'),
        headers: new Headers()
    };
    return pipe(base, ...transformers.filter(isntFalsy));
}
//# sourceMappingURL=pipe-request-options-transformers.js.map