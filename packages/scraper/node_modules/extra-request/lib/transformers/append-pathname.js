import * as URL from 'url-operator';
export function appendPathname(pathname) {
    return (options) => {
        return {
            ...options,
            url: URL.appendPathname(options.url, pathname)
        };
    };
}
//# sourceMappingURL=append-pathname.js.map