export async function* joinAsync(iterable, separator) {
    let first = true;
    for await (const element of iterable) {
        if (first) {
            first = false;
        }
        else {
            yield separator;
        }
        yield element;
    }
}
//# sourceMappingURL=join-async.js.map