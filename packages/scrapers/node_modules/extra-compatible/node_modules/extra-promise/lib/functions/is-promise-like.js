import { isObject, isFunction } from 'extra-utils';
export function isPromiseLike(val) {
    return isObject(val)
        && isFunction(val.then);
}
export function isntPromiseLike(val) {
    return !isPromiseLike(val);
}
//# sourceMappingURL=is-promise-like.js.map