import { SyncExecutor } from './sync-executor.js';
export declare class SyncConstructor<Args extends unknown[] = []> extends SyncExecutor<Args> {
    protected iterate(callbacks: Array<(...args: Args) => unknown>): ((...args: Args) => unknown)[];
}
