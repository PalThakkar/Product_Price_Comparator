export type ICallback<Yield, Next, Args extends unknown[]> = (...args: Args) => void | Generator<Yield, void, Next>;
export declare abstract class GeneratorExecutor<Yield, Next, Args extends unknown[]> {
    private autoClear;
    private callbacks;
    constructor({ autoClear }?: {
        autoClear?: boolean;
    });
    get size(): number;
    defer(callback: ICallback<Yield, Next, Args>): void;
    remove(callback: ICallback<Yield, Next, Args>): void;
    clear(): void;
    execute(...args: Args): Generator<Yield, void, Next>;
    executeSettled(...args: Args): Generator<Yield, void, Next>;
    protected abstract iterate(callbacks: Array<ICallback<Yield, Next, Args>>): Iterable<ICallback<Yield, Next, Args>>;
}
