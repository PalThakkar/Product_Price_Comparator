export async function someAsync(iterable, predicate) {
    let index = 0;
    for await (const element of iterable) {
        if (await predicate(element, index))
            return true;
        index++;
    }
    return false;
}
//# sourceMappingURL=some-async.js.map