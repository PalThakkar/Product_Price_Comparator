export async function* takeUntilAsync(iterable, predicate) {
    let index = 0;
    for await (const element of iterable) {
        if (await predicate(element, index))
            break;
        yield element;
        index++;
    }
}
//# sourceMappingURL=take-until-async.js.map