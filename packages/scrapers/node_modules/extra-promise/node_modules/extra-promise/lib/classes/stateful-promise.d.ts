export declare enum StatefulPromiseState {
    Pending = "pending",
    Fulfilled = "fulfilled",
    Rejected = "rejected"
}
export declare class StatefulPromise<T> extends Promise<T> {
    static from<T>(promise: PromiseLike<T>): StatefulPromise<T>;
    private fsm;
    get state(): StatefulPromiseState;
    constructor(executor: (resolve: (value: T) => void, reject: (reason: any) => void) => void);
    isPending(): boolean;
    isFulfilled(): boolean;
    isRejected(): boolean;
}
