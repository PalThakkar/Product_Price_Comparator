import { setImmediate } from 'extra-timers';
export function goMacrotask(fn) {
    return new Promise((resolve, reject) => {
        setImmediate(async () => {
            try {
                resolve(await fn());
            }
            catch (e) {
                reject(e);
            }
        });
    });
}
//# sourceMappingURL=go-macrotask.js.map