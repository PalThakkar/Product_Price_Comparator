import { setSearchParams } from 'url-operator';
export function searchParams(searchParams) {
    return (options) => {
        return {
            ...options,
            url: setSearchParams(options.url, searchParams)
        };
    };
}
//# sourceMappingURL=search-params.js.map