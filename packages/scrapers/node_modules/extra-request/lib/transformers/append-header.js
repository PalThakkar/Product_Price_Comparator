import { Headers } from 'extra-fetch';
export function appendHeader(name, value) {
    return (options) => {
        const headers = new Headers(options.headers);
        headers.append(name, value);
        return {
            ...options,
            headers
        };
    };
}
//# sourceMappingURL=append-header.js.map