export function url(...urls) {
    return (options) => {
        const newURL = new URL([options.url, ...urls].reduce((acc, cur) => new URL(cur, acc)));
        return {
            ...options,
            url: newURL
        };
    };
}
//# sourceMappingURL=url.js.map