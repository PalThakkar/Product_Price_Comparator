export async function* tapAsync(iterable, fn) {
    let index = 0;
    for await (const element of iterable) {
        await fn(element, index);
        yield element;
        index++;
    }
}
//# sourceMappingURL=tap-async.js.map