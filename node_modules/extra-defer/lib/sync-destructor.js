import { SyncExecutor } from './sync-executor.js';
import { reverse } from 'extra-generator';
export class SyncDestructor extends SyncExecutor {
    iterate(callbacks) {
        return reverse(callbacks);
    }
}
//# sourceMappingURL=sync-destructor.js.map