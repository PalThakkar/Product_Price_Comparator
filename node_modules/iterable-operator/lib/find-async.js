export async function findAsync(iterable, predicate) {
    let index = 0;
    for await (const element of iterable) {
        if (await predicate(element, index))
            return element;
        index++;
    }
    return undefined;
}
//# sourceMappingURL=find-async.js.map