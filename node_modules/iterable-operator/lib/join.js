export function* join(iterable, separator) {
    let first = true;
    for (const element of iterable) {
        if (first) {
            first = false;
        }
        else {
            yield separator;
        }
        yield element;
    }
}
//# sourceMappingURL=join.js.map