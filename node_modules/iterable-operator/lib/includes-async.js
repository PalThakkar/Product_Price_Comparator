export async function includesAsync(iterable, value) {
    for await (const element of iterable) {
        if (element === value)
            return true;
    }
    return false;
}
//# sourceMappingURL=includes-async.js.map