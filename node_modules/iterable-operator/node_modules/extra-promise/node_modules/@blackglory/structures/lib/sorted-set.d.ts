export declare class SortedSet<T> implements Iterable<T> {
    private skipList;
    get [Symbol.toStringTag](): string;
    [Symbol.iterator](): IterableIterator<T>;
    constructor(compare: (a: T, b: T) => number);
    values(): IterableIterator<T>;
    has(value: T): boolean;
    add(value: T): void;
    delete(value: T): void;
}
