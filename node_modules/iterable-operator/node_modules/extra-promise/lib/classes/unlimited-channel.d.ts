import { INonBlockingChannel } from "../utils/types.js";
export { ChannelClosedError } from "../utils/errors.js";
export declare class UnlimitedChannel<T> implements INonBlockingChannel<T> {
    private fsm;
    private enqueueDeferredGroup;
    private buffer;
    send(value: T): void;
    receive(): AsyncIterable<T>;
    close(): void;
}
