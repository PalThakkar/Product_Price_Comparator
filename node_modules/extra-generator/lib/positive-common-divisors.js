import { positiveFactors } from './positive-factors.js';
export function* positiveCommonDivisors(a, b) {
    if (a !== 0 && b !== 0) {
        yield* positiveFactors(gcd(a, b));
    }
}
function gcd(a, b) {
    while (b !== 0) {
        const temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
//# sourceMappingURL=positive-common-divisors.js.map