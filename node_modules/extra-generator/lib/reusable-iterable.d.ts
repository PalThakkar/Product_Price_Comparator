interface IReusableIterable<T> extends Iterable<T> {
    close(): void;
}
export declare class ReusableIterable<T> implements IReusableIterable<T> {
    private _done;
    private getIterator;
    get done(): boolean | undefined;
    constructor(iterable: Iterable<T>);
    close(): void;
    [Symbol.iterator](): {
        next: () => {
            value: any;
            done: boolean | undefined;
        };
    };
}
export {};
