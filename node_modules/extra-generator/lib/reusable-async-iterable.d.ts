interface IReusableAsyncIterable<T> extends AsyncIterable<T> {
    close(): Promise<void>;
}
export declare class ReusableAsyncIterable<T> implements IReusableAsyncIterable<T> {
    private _done;
    private getIterator;
    get done(): boolean | undefined;
    constructor(iterable: AsyncIterable<T>);
    close(): Promise<void>;
    [Symbol.asyncIterator](): {
        next: () => Promise<{
            value: any;
            done: boolean | undefined;
        }>;
    };
}
export {};
