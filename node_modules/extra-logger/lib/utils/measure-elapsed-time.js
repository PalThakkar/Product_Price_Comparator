export function measureElapsedTime(fn, callback) {
    const startTime = Date.now();
    const result = fn();
    const endTime = Date.now();
    callback(endTime - startTime);
    return result;
}
export async function measureElapsedTimeAsync(fn, callback) {
    const startTime = Date.now();
    const result = await fn();
    const endTime = Date.now();
    await callback(endTime - startTime);
    return result;
}
//# sourceMappingURL=measure-elapsed-time.js.map