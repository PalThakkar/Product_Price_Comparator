import { isntNull, isntUndefined, isFunction } from 'extra-utils';
export function isAsyncIterable(val) {
    return isntNull(val)
        && isntUndefined(val)
        && isFunction(val[Symbol.asyncIterator]);
}
export function isntAsyncIterable(val) {
    return !isAsyncIterable(val);
}
//# sourceMappingURL=is-async-iterable.js.map