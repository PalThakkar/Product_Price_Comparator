export async function* mapAsync(iterable, fn) {
    let index = 0;
    for await (const element of iterable) {
        yield await fn(element, index);
        index++;
    }
}
//# sourceMappingURL=map-async.js.map