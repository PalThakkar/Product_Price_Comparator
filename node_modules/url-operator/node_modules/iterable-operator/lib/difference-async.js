import { filterAsync } from "./filter-async.js";
import { toSetAsync } from "./to-set-async.js";
export async function* differenceAsync(left, right) {
    const rightSet = await toSetAsync(right);
    yield* filterAsync(left, async (x) => !rightSet.has(x));
}
//# sourceMappingURL=difference-async.js.map