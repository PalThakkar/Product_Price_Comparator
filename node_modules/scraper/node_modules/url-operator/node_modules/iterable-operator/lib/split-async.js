export async function* splitAsync(iterable, separator) {
    let buffer = [];
    for await (const element of iterable) {
        if (element === separator) {
            yield buffer;
            buffer = [];
        }
        else {
            buffer.push(element);
        }
    }
    yield buffer;
}
//# sourceMappingURL=split-async.js.map