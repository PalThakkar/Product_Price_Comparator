export function* split(iterable, separator) {
    let buffer = [];
    for (const element of iterable) {
        if (element === separator) {
            yield buffer;
            buffer = [];
        }
        else {
            buffer.push(element);
        }
    }
    yield buffer;
}
//# sourceMappingURL=split.js.map