import { IBlockingChannel } from "../utils/types.js";
export { ChannelClosedError } from "../utils/errors.js";
export declare class Channel<T> implements IBlockingChannel<T> {
    private fsm;
    private writeLock;
    private writeDeferredGroup;
    private readDeferredGroup;
    private queue;
    send(value: T): Promise<void>;
    receive(): AsyncIterable<T>;
    close(): void;
}
