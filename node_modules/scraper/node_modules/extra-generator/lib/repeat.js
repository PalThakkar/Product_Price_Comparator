import { go, assert } from '@blackglory/prelude';
export function repeat(val, times = Infinity) {
    assert(times === Infinity || Number.isInteger(times), 'The parameter times must be an integer');
    return go(function* () {
        while (times-- > 0) {
            yield val;
        }
    });
}
//# sourceMappingURL=repeat.js.map