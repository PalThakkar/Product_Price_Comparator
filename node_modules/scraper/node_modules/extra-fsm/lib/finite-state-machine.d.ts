import { IFiniteStateMachineSchema } from './types.js';
export declare class FiniteStateMachine<State extends string | number | symbol, Event extends string | number | symbol> {
    protected schema: IFiniteStateMachineSchema<State, Event>;
    private _state;
    get [Symbol.toStringTag](): string;
    get state(): State;
    constructor(schema: IFiniteStateMachineSchema<State, Event>, initialState: State);
    matches(state: State): boolean;
    can(event: Event): boolean;
    send(event: Event): void;
}
