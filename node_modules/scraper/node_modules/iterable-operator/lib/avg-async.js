export async function avgAsync(iterable) {
    let count = 0;
    let sum = 0;
    for await (const element of iterable) {
        sum += element;
        count++;
    }
    return sum / count;
}
//# sourceMappingURL=avg-async.js.map