export async function eachAsync(iterable, fn) {
    let index = 0;
    for await (const element of iterable) {
        await fn(element, index);
        index++;
    }
}
//# sourceMappingURL=each-async.js.map