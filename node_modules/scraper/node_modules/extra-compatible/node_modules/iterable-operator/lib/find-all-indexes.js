import { pipe } from 'extra-utils';
import { map } from "./map.js";
import { filter } from "./filter.js";
export function findAllIndexes(iterable, predicate) {
    return pipe(iterable, iterable => map(iterable, (x, i) => [x, i]), iterable => filter(iterable, ([x, i]) => predicate(x, i)), iterable => map(iterable, ([_, i]) => i));
}
//# sourceMappingURL=find-all-indexes.js.map