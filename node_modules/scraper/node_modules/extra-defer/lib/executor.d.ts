import { Awaitable } from '@blackglory/prelude';
export declare abstract class Executor<Args extends unknown[]> {
    private autoClear;
    private callbacks;
    constructor({ autoClear }?: {
        autoClear?: boolean;
    });
    get size(): number;
    defer(callback: (...args: Args) => Awaitable<unknown>): void;
    remove(callback: (...args: Args) => Awaitable<unknown>): void;
    clear(): void;
    execute(...args: Args): Promise<void>;
    executeSettled(...args: Args): Promise<void>;
    all(concurrency?: number, ...args: Args): Promise<void>;
    allSettled(concurrency?: number, ...args: Args): Promise<void>;
    protected abstract iterate(callbacks: Array<(...args: Args) => Awaitable<unknown>>): Iterable<(...args: Args) => Awaitable<unknown>>;
}
