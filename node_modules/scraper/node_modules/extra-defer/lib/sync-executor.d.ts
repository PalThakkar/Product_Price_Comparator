export declare abstract class SyncExecutor<Args extends unknown[]> {
    private autoClear;
    private callbacks;
    constructor({ autoClear }?: {
        autoClear?: boolean;
    });
    get size(): number;
    defer(callback: (...args: Args) => unknown): void;
    remove(callback: (...args: Args) => unknown): void;
    clear(): void;
    execute(...args: Args): void;
    executeSettled(...args: Args): void;
    protected abstract iterate(callbacks: Array<(...args: Args) => unknown>): Iterable<(...args: Args) => unknown>;
}
