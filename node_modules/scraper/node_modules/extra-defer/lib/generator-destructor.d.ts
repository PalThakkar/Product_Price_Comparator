import { GeneratorExecutor, ICallback } from './generator-executor.js';
export declare class GeneratorDestructor<Yield = unknown, Next = unknown, Args extends unknown[] = []> extends GeneratorExecutor<Yield, Next, Args> {
    protected iterate(callbacks: Array<ICallback<Yield, Next, Args>>): IterableIterator<ICallback<Yield, Next, Args>>;
}
