import { SyncExecutor } from './sync-executor.js';
export declare class SyncDestructor<Args extends unknown[] = []> extends SyncExecutor<Args> {
    protected iterate(callbacks: Array<(...args: Args) => unknown>): IterableIterator<(...args: Args) => unknown>;
}
