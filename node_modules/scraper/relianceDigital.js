const puppeteer = require("puppeteer");
const fs = require("fs");
const path = require("path");

async function scrapeRelianceDigitalSearch(query, max = 8, opts = {}) {
  // will fill step by step
}

module.exports = { scrapeRelianceDigitalSearch };
