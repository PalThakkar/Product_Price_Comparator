import * as crypto from 'crypto';
export async function hash(algorithm, input) {
    return crypto.createHash(algorithm)
        .update(input)
        .digest('hex');
}
//# sourceMappingURL=hash.js.map