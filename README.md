# Product_Price_Comparator
/frontend        # Next.js app
/backend         # Express API + workers
/scrapers        # scraper modules (cheerio + puppeteer)
/workers         # bullmq workers (alert evaluation, notifications)
/infrastructure  # docker-compose, k8s manifests, terraform (later)
/docs            # architecture, API contract, runbooks
/scripts         # seed scripts, db tools
